name: snowflake_finance_mart
version: "1.0.0"
profile: snowflake_finance_mart
model-paths: ["models"]
macro-paths: ["macros"]

models:
  snowflake_finance_mart:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
